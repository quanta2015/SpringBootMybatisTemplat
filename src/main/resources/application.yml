server:
  port: 80
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://121.40.124.170:3306/shop
    username: hznu
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl